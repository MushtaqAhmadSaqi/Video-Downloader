services:
  multivid:
    build: .
    container_name: multivid
    ports:
      - "5000:5000"
    volumes:
      - ./temp:/app/temp
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
